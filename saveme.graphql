type Device @model @searchable {
  id: ID!
  serialNumber: String!
  group: String!
  deviceId: String!
  activationCode: String
  activated: String
  name: String!
  type: String
  endpoint: String
  location: GPS
}
type GPS @model {
  lon: Float,
  lat: Float
}
